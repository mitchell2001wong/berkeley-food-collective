<%= form_with(model: usr_email) do |form| %>
  <% if usr_email.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usr_email.errors.count, "error") %> prohibited this usr_email from being saved:</h2>

      <ul>
        <% usr_email.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
